# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: harness-mcp-server
  namespace: harness-mcp
spec:
  selector:
    app: harness-mcp-server
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP